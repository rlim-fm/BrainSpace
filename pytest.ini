[pytest]
testpaths = tests
markers =
    slow: end-to-end tests that train models or render with FFmpeg (deselect with -m "not slow")
    cuda: tests requiring a CUDA device (skipped automatically when unavailable)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
